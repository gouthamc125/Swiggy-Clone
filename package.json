{
  "name": "swiggy-clone",
  "version": "1.0.0",
  "description": "Swiggy clone application",
  "main": "index.js",
  "scripts": {
    "build": "node build",
    "start": "node start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "heroku": "^7.59.2"
  }
}
